// Code your solutions in this file
const writeCards = (["Ada", "Brendan", "Ali"], "birthday");
 
function writeCards(cards) {
  for (let i = 0; i < cards.length; i++) {
    console.log(`Thank you ${cardss[i]} for the wonderful birthday gift!`);
  }
 
  return cards;
}